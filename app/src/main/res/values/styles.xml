<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNavy</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_border</item>
    </style>

    <style name="Widget.TichComponent.LoadingView" parent="">
        <item name="indicator">@drawable/oval</item>
        <item name="indicatorTint">?attr/colorPrimary</item>
        <item name="indicatorRadius">6dp</item>
        <item name="indicatorPadding">8dp</item>
    </style>

    <!--Date Picker divider-->
    <style name="AppTheme.DatePicker" parent="AppTheme">
        <item name="colorControlNormal">@color/colorBlue</item>
    </style>
</resources>